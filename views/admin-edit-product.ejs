<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Product: <%= product.name %></title>
  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="bg-light">

  <%- include('partials/header') %>

  <div class="container my-5">
    <h2 class="text-center mb-4">Edit Product: <%= product.name %></h2>

    <form action="/admin/products/edit/<%= product.id %>" method="POST" class="bg-white p-4 shadow rounded">

      <!-- Product Name -->
      <div class="mb-3">
        <label class="form-label">Product Name</label>
        <input type="text" class="form-control" name="name" value="<%= product.name %>" required>
      </div>

      <!-- Price -->
      <div class="mb-3">
        <label class="form-label">Price</label>
        <input type="number" step="0.01" class="form-control" name="price" value="<%= product.price %>" required>
      </div>

      <!-- Image URL -->
      <div class="mb-3">
        <label class="form-label">Image URL</label>
        <input type="text" class="form-control" name="image" value="<%= product.image %>">
      </div>

      <!-- Main Category -->
      <div class="mb-3">
        <label class="form-label">Main Category</label>
        <input type="text" class="form-control" name="main_category" value="<%= product.main_category %>">
      </div>

      <!-- Sub Category -->
      <div class="mb-3">
        <label class="form-label">Sub-Category</label>
        <input type="text" class="form-control" name="sub_category" value="<%= product.sub_category %>">
      </div>

      <!-- Sub Category Image -->
      <div class="mb-3">
        <label class="form-label">Sub-Category Image</label>
        <input type="text" class="form-control" name="sub_category_image" value="<%= product.sub_category_image %>">
      </div>

      <div class="mb-3">
  <label for="notes" class="form-label">Product Notes (Optional)</label>
  <textarea class="form-control" id="notes" name="notes" rows="3"><%= product.notes || '' %></textarea>
</div>


      <div class="mb-3">
  <label for="player_id_label" class="form-label">Player ID Label (اختياري)</label>
  <input type="text" class="form-control" id="player_id_label" name="player_id_label" value="<%= product.player_id_label || '' %>">
</div>

      <!-- Requires Player ID -->
      <div class="form-check mb-4">
        <input type="checkbox" class="form-check-input" id="requiresPlayerId" name="requires_player_id" value="1"
          <% if (product.requires_player_id == 1) { %> checked <% } %>>
        <label class="form-check-label" for="requiresPlayerId">Requires Player ID</label>
      </div>

      <button type="submit" class="btn btn-success w-100">💾 Save Changes</button>
    </form>
  </div>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
