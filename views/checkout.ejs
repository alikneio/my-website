<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - <%= product.name %></title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { background-color: #0d1117; color: #c9d1d9; font-family: 'Cairo', sans-serif; }
        .checkout-container { max-width: 500px; margin: 5rem auto; padding: 2rem; background-color: #161b22; border: 1px solid #30363d; border-radius: 15px; }
        .product-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; border-bottom: 1px solid #30363d; padding-bottom: 1rem; }
        .product-header .price { background-color: #238636; color: white; padding: 5px 15px; border-radius: 50px; font-weight: bold; }
        .form-control { background-color: #0d1117; border: 1px solid #30363d; color: white; padding: 1rem; border-radius: 30px; font-size: 1rem; }
        .form-control::placeholder { color: #8b949e; }
        .form-control:focus { background-color: #161b22; border-color: #1f6feb; box-shadow: 0 0 0 0.25rem rgba(31, 111, 235, 0.25); color: white; }
        .btn-buy { background: linear-gradient(90deg, #1f6feb, #2d96d9); border: 0; border-radius: 30px; padding: 1rem; font-weight: bold; font-size: 1.1rem; width: 100%; }
    </style>
</head>
<body>

    <div class="checkout-container">
        <div class="product-header">
            <h4 class="mb-0"><%= product.name %></h4>
            <span class="price">$<%= product.price.toFixed(2) %></span>
        </div>

        <form id="checkout-form">
            <input type="hidden" name="productName" value="<%= product.name %>">
            <input type="hidden" name="price" value="<%= product.price %>">

            <% if (product.requires_player_id === 1) { %>
                <div class="mb-3">
                    <label for="player_id" class="form-label">Player ID</label>
                    <input type="text" class="form-control" name="player_id" id="player_id" placeholder="Enter the required ID" required>
                </div>
            <% } %>
            
            <div class="d-grid mt-4">
                <button type="submit" class="btn btn-primary btn-buy">Complete Purchase</button>
            </div>
        </form>
    </div>
    
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
      <div id="responseToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-header"><strong class="me-auto" id="toastTitle"></strong><button type="button" class="btn-close" data-bs-dismiss="toast"></button></div>
        <div class="toast-body" id="toastBody"></div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/main.js"></script>
</body>
</html>