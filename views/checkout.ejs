<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Checkout - <%= product.name %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css">

  <style>
    .product-img {
      max-height: 120px;
      object-fit: contain;
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <div class="container my-5">
    <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-5 mx-auto">
      <div class="card p-4 shadow-sm">

        <!-- صورة المنتج -->
        <img
          src="<%= product.image %>"
          alt="<%= product.name %>"
          class="img-fluid mb-3 product-img"
          loading="lazy"
        >

        <!-- اسم المنتج والسعر -->
        <h4 class="fw-bold mb-1"><%= product.name %></h4>
        <p class="fs-5 text-success mb-3">$<%= Number(product.price).toFixed(2) %></p>

        <!-- ملاحظات المنتج إن وجدت -->
        <% if (product.notes) { %>
          <div class="alert alert-warning small mb-4">
            <strong>📝 Note:</strong> <%= product.notes %>
          </div>
        <% } %>

        <!-- ✅ نموذج الشراء -->
        <form
          id="sql-buy-form"
          method="POST"
          action="/buy"
          enctype="multipart/form-data"
          novalidate
        >
          <input type="hidden" name="productId" value="<%= product.id %>">
          <input type="hidden" name="idempotency_key" value="<%= idemKey %>">

          <% if (product.requires_player_id == 1) { %>
            <div class="mb-3">
              <label for="playerId" class="form-label">
                <%= product.player_id_label || 'Player ID' %>
              </label>

              <input
                type="text"
                class="form-control"
                id="playerId"
                name="playerId"
                placeholder="Enter <%= product.player_id_label || 'Player ID' %>"
                required
                autocomplete="off"
                inputmode="text"
              >

              <div class="invalid-feedback">
                Please enter <%= product.player_id_label || 'Player ID' %>.
              </div>
            </div>
          <% } %>

          <button id="buyBtn" type="submit" class="btn btn-success w-100">
            <span class="btn-text">Buy Now</span>
            <span class="spinner-border spinner-border-sm ms-2 d-none" role="status" aria-hidden="true"></span>
          </button>

          <p class="text-muted small mt-3 mb-0">
            By clicking “Buy Now”, your order will be processed securely.
          </p>
        </form>

      </div>
    </div>
  </div>

  <%- include('partials/footer') %>

  <!-- SweetAlert2 -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <!-- Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    const form = document.getElementById('sql-buy-form');
    const buyBtn = document.getElementById('buyBtn');
    const btnText = buyBtn.querySelector('.btn-text');
    const btnSpinner = buyBtn.querySelector('.spinner-border');

    let submitting = false;

    function setLoading(state) {
      submitting = state;
      buyBtn.disabled = state;

      if (state) {
        btnSpinner.classList.remove('d-none');
        btnText.textContent = 'Processing...';
      } else {
        btnSpinner.classList.add('d-none');
        btnText.textContent = 'Buy Now';
      }
    }

    function showError(message) {
      Swal.fire({
        icon: 'error',
        title: 'خطأ',
        text: message || 'حدث خطأ أثناء تنفيذ العملية.'
      });
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (submitting) return;

      // ✅ Bootstrap validation (خصوصًا playerId)
      if (!form.checkValidity()) {
        form.classList.add('was-validated');
        return;
      }

      setLoading(true);

      try {
        const formData = new FormData(form);

        const res = await fetch(form.action || '/buy', {
          method: 'POST',
          body: formData,
          headers: { 'Accept': 'application/json' },
          // ✅ يمنع المتصفح يتبع redirect تلقائيًا (حتى ما يودّي المستخدم /processing بالغلط)
          redirect: 'manual'
        });

        // إذا السيرفر رجّع 3xx Redirect بدل JSON
        if (res.status >= 300 && res.status < 400) {
          setLoading(false);
          showError('السيرفر رجّع Redirect بدل JSON. لازم /buy يرجّع JSON فقط.');
          return;
        }

        const contentType = (res.headers.get('content-type') || '').toLowerCase();

        if (!contentType.includes('application/json')) {
          const text = await res.text().catch(() => '');
          console.error('Non-JSON response:', text);
          setLoading(false);
          showError('رد غير متوقع من السيرفر (ليس JSON). تأكد من روت /buy.');
          return;
        }

        const data = await res.json();

        // إذا HTTP error أو success=false
        if (!res.ok || !data?.success) {
          setLoading(false);
          showError(data?.message || 'تعذّر إتمام العملية. تأكد من الرصيد أو البيانات.');
          return;
        }

        // نجاح: نوجّه حسب ما يرجعه السيرفر
        const nextUrl = data.redirectUrl || '/processing';
        window.location.href = nextUrl;

      } catch (err) {
        console.error(err);
        setLoading(false);
        showError('تعذر تنفيذ الطلب، تأكد من الاتصال بالإنترنت.');
      }
    });
  </script>

</body>
</html>
