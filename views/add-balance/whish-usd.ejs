<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Whish Money - Add USD Balance</title>
  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Cairo', sans-serif;
      background-color: #f8f9fa;
    }
    .form-wrapper {
      max-width: 500px;
      margin: 40px auto;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.1);
      padding: 30px;
    }
    .title {
      text-align: center;
      font-weight: bold;
      margin-bottom: 25px;
    }
    .note-box {
      background: #f1f1f1;
      border-left: 5px solid #007bff;
      padding: 10px 15px;
      margin-bottom: 20px;
    }
    .custom-file-input::file-selector-button {
      padding: 0.4em 1em;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      margin-right: 10px;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>




    <div class="form-wrapper">
      <h2 class="title">Recharge via Whish Money (USD)</h2>

      <div class="note-box">
        <p>💸 Please transfer the amount to <strong>Whish Number: 71534662</strong></p>
        <p>📌 Minimum recharge: <strong>$5</strong></p>
      </div>

      <form action="/add-balance/whish/usd" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="amount" class="form-label">Amount (USD)</label>
          <input type="number" name="amount" id="amount" class="form-control" min="5" required>
        </div>

        <div class="mb-3">
          <label for="proof" class="form-label">Upload Proof of Payment</label>
        <input type="file" name="proofImage" id="proofImage" accept="image/*" class="form-control" required>


        </div>

        <button type="submit" class="btn btn-primary w-100">Submit Request</button>
      </form>
    </div>
  </div>

 <%- include('../partials/footer') %>


</body>
</html>