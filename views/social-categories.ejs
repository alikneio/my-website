<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Social Media Services</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="/css/custom.css" />
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css" />
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --brand: #1266F1;
      --brand-soft: rgba(18, 102, 241, 0.08);
      --bg-soft: #f4f6fb;
      --text-main: #1b2330;
      --text-muted: #6c757d;
      --card-radius: 18px;
      --card-shadow: 0 18px 35px rgba(15, 23, 42, 0.08);
    }

    body {
      background: radial-gradient(circle at top left, #e7f0ff 0, #f6f8ff 40%, #f4f6fb 100%);
      font-family: "Cairo", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      color: var(--text-main);
    }

    .page-header {
      text-align: center;
      padding: 2.2rem 0 1.2rem;
    }

    .page-title {
      font-weight: 800;
      letter-spacing: .03em;
      font-size: 1.9rem;
      color: #111827;
    }

    .page-subtitle {
      color: var(--text-muted);
      max-width: 540px;
      margin: .45rem auto 0;
      font-size: .96rem;
    }

    /* ===== GRID ===== */
    .categories-grid {
      display: grid;
      gap: 1.4rem;
      margin-bottom: 3rem;
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    /* Tablet & down: 2 كروت بالصف */
    @media (max-width: 991.98px) {
      .categories-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 1.1rem;
      }
    }

    /* ===== CARD ===== */
    .category-card {
      position: relative;
      background: rgba(255, 255, 255, 0.97);
      border-radius: var(--card-radius);
      box-shadow: var(--card-shadow);
      padding: 1.4rem 1.45rem 1.1rem;
      border: 1px solid rgba(148, 163, 184, 0.18);
      backdrop-filter: blur(14px);
      transition:
        transform .18s ease,
        box-shadow .18s ease,
        border-color .18s ease,
        background .18s ease;
      display: flex;
      flex-direction: column;
      min-height: 220px;
      overflow: hidden;
    }

    .category-card::before {
      content: "";
      position: absolute;
      inset: 0;
      background: linear-gradient(
        135deg,
        rgba(59, 130, 246, .13),
        rgba(129, 140, 248, .06)
      );
      opacity: 0;
      pointer-events: none;
      transition: opacity .2s ease;
    }

    .category-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 22px 45px rgba(15, 23, 42, 0.16);
      border-color: rgba(37, 99, 235, 0.35);
    }

    .category-card:hover::before {
      opacity: 1;
    }

    .category-icon {
      width: 52px;
      height: 52px;
      border-radius: 20px;
      background: radial-gradient(circle at 30% 20%, #e0ecff 0, #c7d2fe 35%, #e0f2fe 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1rem;
      color: #0f172a;
      position: relative;
      z-index: 1;
    }

    .category-icon i {
      font-size: 1.6rem;
    }

    .category-badges {
      display: flex;
      flex-wrap: wrap;
      gap: .35rem;
      margin-bottom: .55rem;
      position: relative;
      z-index: 1;
    }

    .badge-soft {
      border-radius: 999px;
      padding: .17rem .55rem;
      font-size: .68rem;
      letter-spacing: .05em;
      text-transform: uppercase;
      font-weight: 700;
      background: rgba(37, 99, 235, 0.08);
      color: #2563eb;
    }

    .badge-new {
      background: rgba(236, 72, 153, 0.12);
      color: #db2777;
    }

    .badge-fast {
      background: rgba(16, 185, 129, 0.12);
      color: #059669;
    }

    .category-title {
      font-size: 1.05rem;
      font-weight: 700;
      margin-bottom: .18rem;
      color: #111827;
      position: relative;
      z-index: 1;
    }

    .category-subtitle {
      font-size: .78rem;
      text-transform: uppercase;
      letter-spacing: .08em;
      color: var(--text-muted);
      margin-bottom: .45rem;
      position: relative;
      z-index: 1;
    }

    .category-description {
      font-size: .84rem;
      line-height: 1.5;
      color: var(--text-muted);
      margin-bottom: .85rem;
      position: relative;
      z-index: 1;
    }

    .category-meta {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-top: auto;
      gap: .6rem;
      position: relative;
      z-index: 1;
      padding-top: .45rem;
      border-top: 1px dashed rgba(148, 163, 184, 0.6);
    }

    .services-pill {
      display: inline-flex;
      align-items: center;
      gap: .35rem;
      font-size: .8rem;
      color: #111827;
    }

    .services-pill-count {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 22px;
      height: 22px;
      border-radius: 999px;
      background: var(--brand-soft);
      color: #1d4ed8;
      font-weight: 700;
      font-size: .8rem;
    }

    .view-link {
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      padding: .3rem .8rem;
      font-size: .8rem;
      color: #111827;
      display: inline-flex;
      align-items: center;
      gap: .3rem;
      text-decoration: none;
      background: #fff;
      transition:
        background .18s ease,
        border-color .18s ease,
        color .18s ease;
      white-space: nowrap;
    }

    .category-card:hover .view-link {
      border-color: transparent;
      background: #2563eb;
      color: #fff;
    }

    .view-link i {
      font-size: .9rem;
    }

    /* Empty state */
    .empty-state {
      text-align: center;
      padding: 4rem 0 3rem;
      color: var(--text-muted);
    }

    .empty-state-icon {
      font-size: 2.6rem;
      margin-bottom: .75rem;
      color: #9ca3af;
    }

    /* ===== Mobile tuning ===== */
    @media (max-width: 767.98px) {
      .page-header {
        padding-top: 1.6rem;
        padding-bottom: 0.8rem;
      }
      .page-title {
        font-size: 1.5rem;
      }
      .page-subtitle {
        font-size: .86rem;
      }
      .category-card {
        padding: 1.05rem 1rem 0.9rem;
        border-radius: 16px;
        box-shadow: 0 12px 24px rgba(15, 23, 42, 0.10);
      }
      .category-icon {
        width: 46px;
        height: 46px;
        margin-bottom: 0.7rem;
      }
      .category-title {
        font-size: .92rem;
      }
      .category-subtitle {
        font-size: .7rem;
      }
      .category-description {
        font-size: .78rem;
        line-height: 1.45;
      }
      .services-pill {
        font-size: .76rem;
      }
      .services-pill-count {
        width: 20px;
        height: 20px;
        font-size: .76rem;
      }
      .view-link {
        padding: .25rem .7rem;
        font-size: .76rem;
      }
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <main class="container my-4 my-md-5">
    <header class="page-header">
      <h1 class="page-title">Social Media Services</h1>
      <p class="page-subtitle">
        Choose a category to browse curated services with optimized pricing and delivery for each platform.
      </p>
    </header>

    <%
      const cats = Array.isArray(categories)
        ? categories
        : (Array.isArray(smmCategories) ? smmCategories : []);
    %>

    <% if (cats.length === 0) { %>
      <section class="empty-state">
        <div class="empty-state-icon">
          <i class="bi bi-collection-play"></i>
        </div>
        <h5 class="mb-1">No categories available yet</h5>
        <p class="mb-0">
          Once social media categories are configured, they’ll appear in this section.
        </p>
      </section>
    <% } else { %>

      <section class="categories-grid">
        <% cats.forEach(function(cat) {
          const title   = cat.label || cat.name || cat.title || 'Social Media Category';
          const slug    = cat.slug || cat.handle || cat.id;
          const count   = Number(
            cat.service_count ||
            cat.services_count ||
            cat.services ||
            cat.servicesCount ||
            cat.total_services ||
            0
          );
          const isNew   = cat.is_new === 1 || cat.is_new === true || cat.isNew;
          const isFast  = cat.is_fast === 1 || cat.is_fast === true || cat.fastDelivery;
          const section = cat.section || cat.platform || '';
        %>

          <article class="category-card">
            <div class="category-icon">
              <i class="bi bi-collection-play"></i>
            </div>

            <div class="category-badges">
              <% if (section) { %>
                <span class="badge-soft"><%= section %></span>
              <% } %>
              <% if (isNew) { %>
                <span class="badge-soft badge-new">New</span>
              <% } %>
              <% if (isFast) { %>
                <span class="badge-soft badge-fast">Fast delivery</span>
              <% } %>
            </div>

            <h2 class="category-title"><%= title %></h2>
            <div class="category-subtitle">
              Curated social media services
            </div>

            <p class="category-description">
              Best options in this category with optimized price, stability and delivery speed.
              Click to see all available services.
            </p>

            <div class="category-meta">
              <div class="services-pill">
                <span class="services-pill-count"><%= count %></span>
                <span><%= count === 1 ? 'service' : 'services' %></span>
              </div>

              <% if (slug) { %>
                <a href="/social-media/<%= slug %>" class="view-link">
                  View services
                  <i class="bi bi-arrow-right-short"></i>
                </a>
              <% } else { %>
                <span class="text-muted small">No link configured</span>
              <% } %>
            </div>
          </article>

        <% }); %>
      </section>
    <% } %>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
