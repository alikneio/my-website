<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Social Media Services</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="/css/custom.css" />
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css" />
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --smm-brand: #1266f1;
      --smm-soft-bg: #f4f6fa;
      --smm-title: #1d3557;
      --smm-muted: #6c757d;
    }

    body {
      font-family: "Cairo", sans-serif;
    }

    .smm-page-wrapper {
      padding-block: 2.5rem 3.5rem;
    }

    .smm-page-header {
      display: flex;
      align-items: center;
      gap: .75rem;
      margin-bottom: 1.5rem;
      flex-wrap: wrap;
    }

    .smm-page-icon {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      background: #ffffff;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 6px 14px rgba(15, 23, 42, .12);
      color: var(--smm-brand);
      font-size: 1.3rem;
    }

    .smm-page-title {
      font-size: 1.9rem;
      font-weight: 800;
      color: var(--smm-title);
      letter-spacing: .2px;
    }

    .smm-page-subtitle {
      color: var(--smm-muted);
      font-size: .95rem;
    }

    /* Card */
    .smm-cat-col {
      margin-bottom: 1.5rem;
    }

    .smm-cat-card {
      display: block;
      position: relative;
      height: 100%;
      padding: 1.6rem 1.4rem 1.35rem;
      border-radius: 1.25rem;
      background: #ffffff;
      box-shadow: 0 10px 26px rgba(15, 23, 42, .06);
      text-decoration: none;
      color: inherit;
      transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
      border: 1px solid rgba(148, 163, 184, .18);
    }

    .smm-cat-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 16px 38px rgba(15, 23, 42, .12);
      border-color: rgba(18, 102, 241, .32);
    }

    .smm-card-icon-wrap {
      width: 50px;
      height: 50px;
      border-radius: 1rem;
      background: #eff4ff;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1rem;
      color: var(--smm-brand);
      font-size: 1.6rem;
    }

    .smm-card-title-row {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: .75rem;
      margin-bottom: .4rem;
    }

    .smm-card-title {
      font-size: 1.05rem;
      font-weight: 700;
      color: #111827;
      line-height: 1.35;
    }

    .smm-badge-new {
      font-size: .7rem;
      font-weight: 700;
      padding: .18rem .5rem;
      border-radius: 999px;
      color: #fff;
      background: linear-gradient(135deg, #f97316, #ec4899);
      text-transform: uppercase;
      letter-spacing: .5px;
      white-space: nowrap;
    }

    .smm-card-meta {
      font-size: .82rem;
      color: var(--smm-muted);
      margin-bottom: .4rem;
    }

    .smm-card-services {
      font-size: .86rem;
      font-weight: 600;
      color: #111827;
      display: inline-flex;
      align-items: center;
      gap: .4rem;
    }

    .smm-card-services span.count-pill {
      padding: .18rem .55rem;
      border-radius: 999px;
      background: #eff4ff;
      color: var(--smm-brand);
      font-size: .8rem;
      font-weight: 700;
    }

    .smm-card-footer {
      margin-top: 1rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: .8rem;
      color: var(--smm-muted);
    }

    .smm-card-footer .chevron {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 26px;
      height: 26px;
      border-radius: 999px;
      background: #eff4ff;
      color: var(--smm-brand);
      font-size: .9rem;
    }

    @media (max-width: 576px) {
      .smm-page-title {
        font-size: 1.6rem;
      }
      .smm-cat-card {
        padding: 1.4rem 1.25rem 1.2rem;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main class="container smm-page-wrapper">

    <!-- Header -->
    <header class="smm-page-header">
      <div class="smm-page-icon">
        <i class="bi bi-instagram"></i>
      </div>
      <div>
        <h1 class="smm-page-title mb-0">Social Media Services</h1>
        <p class="smm-page-subtitle mb-0">
          Choose a category to see all available services with real-time pricing and delivery speed.
        </p>
      </div>
    </header>

    <!-- Categories grid -->
    <div class="row">
      <% if (categories && categories.length) { %>
        <% categories.forEach(function(cat){ 
             // عدّل الأسماء حسب أعمدة جدولك لو مختلفة
             const slug  = cat.slug  || cat.alias || cat.id;
             const name  = cat.name  || cat.label;
             const count = cat.services_count || cat.servicesCount || cat.count || 0;
             const isNew = cat.is_new || cat.isNew || cat.tag === 'NEW';
        %>
          <div class="col-12 col-sm-6 col-lg-3 smm-cat-col">
            <a href="/social-media/<%= slug %>" class="smm-cat-card">
              <div class="smm-card-icon-wrap">
                <i class="bi bi-collection-play-fill"></i>
              </div>

              <div class="smm-card-title-row">
                <h2 class="smm-card-title">
                  <%= name %>
                </h2>
                <% if (isNew) { %>
                  <span class="smm-badge-new">NEW</span>
                <% } %>
              </div>

              <div class="smm-card-meta">
                Curated services for this category with best price &amp; speed.
              </div>

              <div class="smm-card-services">
                <span class="count-pill"><%= count %></span>
                <span><%= count === 1 ? 'service' : 'services' %></span>
              </div>

              <div class="smm-card-footer">
                <span>Click to view all services</span>
                <span class="chevron"><i class="bi bi-chevron-right"></i></span>
              </div>
            </a>
          </div>
        <% }); %>
      <% } else { %>
        <div class="col-12 text-center py-5 text-muted">
          <i class="bi bi-inbox display-6 d-block mb-2"></i>
          <p class="mb-1">No social media categories found.</p>
          <small>Once you add categories from admin, they will appear here automatically.</small>
        </div>
      <% } %>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
