<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Order Details - #<%= order.id %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">

  <style>
    body {
      background-color: #f4f6fa;
      font-family: 'Cairo', sans-serif;
      color: #222;
    }

    .order-wrapper {
      background-color: #fff;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0 0 20px rgba(0,0,0,0.05);
    }

    .section-title {
      font-size: 2rem;
      font-weight: 700;
      color: #1d3557;
    }

    .order-label {
      font-weight: 600;
      color: #495057;
    }

    .order-value {
      background-color: #f1f3f5;
      border-radius: 8px;
      padding: 0.75rem 1rem;
      font-size: 1rem;
      margin-bottom: 1.5rem;
      color: #212529;
    }

    .badge {
      font-size: 1rem;
      padding: 0.4em 1em;
    }

    .btn-back {
      margin-top: 1rem;
    }

    @media (max-width: 768px) {
      .section-title {
        font-size: 1.5rem;
      }
      .order-value {
        font-size: 0.95rem;
      }
    }
  </style>
</head>
<body>

<%- include('partials/header') %>

<main class="container my-5">
  <div class="order-wrapper">
    <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
      <h2 class="section-title">Order Details</h2>
      <div>
        <% if (order.status === 'Accepted') { %>
          <span class="badge bg-success">Accepted</span>
        <% } else if (order.status === 'Rejected') { %>
          <span class="badge bg-danger">Rejected</span>
        <% } else { %>
          <span class="badge bg-warning text-dark">Waiting</span>
        <% } %>
      </div>
    </div>

    <div>
      <div class="order-label">ORDER ID</div>
      <div class="order-value"><%= order.id %></div>

      <div class="order-label">PRODUCT</div>
      <div class="order-value"><%= order.productName %></div>

      <div class="order-label">PRICE</div>
      <div class="order-value">$<%= parseFloat(order.price).toFixed(2) %></div>

      <div class="order-label">DATE</div>
      <div class="order-value"><%= new Date(order.purchaseDate).toLocaleString() %></div>

      <% if (order.order_details) { %>
        <div class="order-label">PLAYER ID</div>
        <div class="order-value"><%= order.order_details %></div>
      <% } %>

      <% if (order.admin_reply) { %>
        <div class="order-label">REPLY</div>
        <div class="order-value text-warning"><%= order.admin_reply %></div>
      <% } %>

      <a href="/my-orders" class="btn btn-outline-primary btn-back"><i class="bi bi-arrow-left"></i> Back</a>
    </div>
  </div>
</main>

<%- include('partials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/main.js"></script>
</body>
</html>
