<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category.label %> products - AK Cell</title>

  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">

  <style>
    /* نفس روح نتفلكس كروت */
    .product-card { border-radius: 1rem; }
    .product-image-wrapper { position: relative; }
    .product-image-wrapper img { width: 100%; height: auto; object-fit: contain; }

    /* تعطيل اللينك لما المنتج Out of Stock */
    .disabled-link { pointer-events: none; }

    /* أوڤرلاي نفاد المخزون */
    .oos-overlay{
      position:absolute; inset:0; display:flex; align-items:center; justify-content:center;
      background:rgba(220,53,69,.28); color:#dc3545; font-weight:800; letter-spacing:.4px;
      border-radius: .75rem;
      text-shadow: 0 1px 2px #fff;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="container my-5">
    <h1 class="text-center mb-4"><%= category.label %> Products</h1>

    <!-- Search bar بنفس ستايلك -->
    <div class="container my-5">
      <div class="search-bar-container">
        <form id="searchForm" action="#" method="GET" role="search">
          <input type="search" id="searchInput" class="search-input" placeholder="Search for any service...">
          <button type="submit" class="search-btn">
            <i class="bi bi-search"></i>
          </button>
        </form>
      </div>
    </div>

    <div class="row g-3">
      <% if (products && products.length > 0) { %>
        <% products.forEach(function(p){ %>
          <div class="col-6 col-sm-4 col-md-3">
            <% const href = user ? `/api-checkout/${p.id}` : '/login'; %>

            <a href="<%= href %>" class="text-decoration-none h-100 <%= p.is_out_of_stock ? 'disabled-link' : '' %>">
              <div class="product-card border-0 shadow-sm rounded-4 overflow-hidden text-center bg-white h-100 animate-zoom">

                <!-- الصورة -->
                <div class="product-image-wrapper mb-2 p-2">
                  <img src="<%= p.image %>" alt="<%= p.name %>" class="img-fluid rounded-3">
                  <% if (p.is_out_of_stock) { %>
                    <div class="oos-overlay">OUT OF STOCK</div>
                  <% } %>
                </div>

                <!-- الاسم -->
                <h6 class="fw-semibold text-dark mb-1"><%= p.name %></h6>

                <!-- السعر (لو منتج متغير الكمية ما نعرض سعر ثابت) -->
                <% if (p.price !== null && p.price !== undefined) { %>
                  <p class="text-primary fw-bold mb-0">$<%= Number(p.price).toFixed(2) %></p>
                <% } else { %>
                  <p class="text-muted fw-semibold mb-0">
                    <%= p.unit_label ? `Variable (${p.unit_label})` : 'Variable quantity' %>
                  </p>
                <% } %>
              </div>
            </a>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12 text-center">
          <p class="text-muted fs-5">No products available in this category.</p>
        </div>
      <% } %>
    </div>
  </div>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- نفس سكربت البحث المعتمد بحساب نتفلكس -->
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const searchInput = document.getElementById("searchInput");
      if (!searchInput) return;

      const searchForm = document.getElementById("searchForm");
      if (searchForm) searchForm.addEventListener("submit", e => e.preventDefault());

      searchInput.addEventListener("input", function () {
        const q = this.value.toLowerCase();
        const cards = document.querySelectorAll(".product-card");
        cards.forEach(card => {
          const title = card.querySelector("h6")?.textContent?.toLowerCase() || "";
          const wrapper = card.closest("div[class^='col']");
          if (wrapper) wrapper.style.display = title.includes(q) ? "" : "none";
        });
      });
    });
  </script>
</body>
</html>
