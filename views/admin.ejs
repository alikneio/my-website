<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Admin Panel</h1>
          <a href="/" class="btn btn-secondary">Back to Home</a>
        </div>


<div class="container mt-5">
    <div class="mt-5">
        <h2>Product Management</h2>
        <p>Manage all products and categories from here.</p>
        <a href="/admin/products" class="btn btn-primary">Go to Products</a>
    </div>
</div>


        <div class="card">
            <div class="card-header">
                <h3>Manage User Balances</h3>
            </div>
            <div class="card-body">
                <form action="/admin/add-credit" method="POST" class="row g-3 align-items-end">
                    <div class="col-md-5">
                        <label for="userId" class="form-label">Select User</label>
                        <select name="userId" id="userId" class="form-select">
                            <% users.forEach(user => { %>
                                <option value="<%= user.id %>"><%= user.username %> (<%= user.email %>) - Balance: $<%= user.balance.toFixed(2) %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="col-md-5">
                        <label for="amount" class="form-label">Amount to Add</label>
                        <input type="number" name="amount" id="amount" class="form-control" placeholder="e.g., 50" required min="1" step="any">
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100">Add Credit</button>
                    </div>
                </form>
            </div>
        </div>

    </div>

</body>
</html>