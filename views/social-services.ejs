<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= category %> - Social Media Services</title>
  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container my-4">
    <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between mb-3">
      <div>
        <h2 class="section-title mb-1"><%= category %></h2>
        <p class="text-muted mb-0">Choose the service you want to order.</p>
      </div>
      <a href="/social-media" class="btn btn-outline-secondary mt-2 mt-md-0">
        <i class="bi bi-arrow-left"></i> Back to categories
      </a>
    </div>

    <% if (!services || services.length === 0) { %>
      <div class="alert alert-warning text-center">
        No services available in this category.
      </div>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-3">
        <% services.forEach(function(s){ %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title"><%= s.name %></h5>

                <div class="small text-muted mb-2">
                  <span class="me-3">
                    <i class="bi bi-cash-coin"></i>
                    Rate: $<%= Number(s.rate).toFixed(4) %> / 1000
                  </span>
                  <span>
                    <i class="bi bi-arrows-expand"></i>
                    Min <%= s.min_qty %> – Max <%= s.max_qty %>
                  </span>
                </div>

                <% if (s.type) { %>
                  <div class="badge bg-primary mb-2">
                    <%= s.type %>
                  </div>
                <% } %>

                <p class="text-muted small mb-3">
                  Enter the link & quantity on the next step to see the exact price.
                </p>

                <div class="mt-auto d-flex justify-content-between align-items-center">
                  <span class="fw-bold">
                    From $<%= (Number(s.rate) * Number(s.min_qty) / 1000).toFixed(2) %>
                  </span>
                  <a href="/social-checkout/<%= s.id %>" class="btn btn-primary">
                    Order <i class="bi bi-arrow-right"></i>
                  </a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/main.js"></script>
</body>
</html>
