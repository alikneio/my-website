<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= categoryName %> – Social Media Services</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <link rel="stylesheet" href="/css/bootstrap.css" />
  <link rel="stylesheet" href="/css/custom.css" />
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css" />
  <link
    href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap"
    rel="stylesheet"
  />
</head>
<body>
  <!-- غيّر header لـ navbar إذا اسم الجزئية عندك مختلف -->
  <%- include('partials/header', { user: user }) %>

  <main class="bg-light py-4">
    <div class="container" style="max-width: 1100px;">

      <!-- العنوان + breadcrumb -->
      <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-3 gap-2">
        <div>
          <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-1">
              <li class="breadcrumb-item">
                <a href="/">Home</a>
              </li>
              <li class="breadcrumb-item">
                <a href="/social-media">Social Media</a>
              </li>
              <li class="breadcrumb-item active" aria-current="page">
                <%= categoryName %>
              </li>
            </ol>
          </nav>
          <h1 class="h4 mb-0">
            <i class="bi bi-instagram"></i>
            <%= categoryName %>
          </h1>
        </div>

        <a href="/social-media" class="btn btn-outline-secondary btn-sm">
          <i class="bi bi-arrow-left"></i> Back to categories
        </a>
      </div>

      <!-- في حال ما في خدمات (احتياطي) -->
      <% if (!services || !services.length) { %>
        <div class="alert alert-warning text-center">
          لا توجد خدمات مفعّلة ضمن هذه الفئة حالياً.
        </div>
      <% } else { %>

        <!-- هنت بسيط عن طريقة الحساب -->
        <div class="alert alert-info small d-flex align-items-start gap-2">
          <i class="bi bi-info-circle mt-1"></i>
          <div>
            الأسعار معروضة <strong>لكل 1000</strong> من الكمية.
            <br />
            السعر النهائي = (الكمية × السعر لكل 1000) ÷ 1000.
          </div>
        </div>

        <!-- لستة الخدمات -->
        <div class="row row-cols-1 row-cols-md-2 g-3">
          <% services.forEach(function(s) { %>
            <div class="col">
              <div class="card h-100 shadow-sm service-card">
                <div class="card-body d-flex flex-column">
                  <!-- اسم الخدمة -->
                  <h2 class="h6 mb-1"><%= s.name %></h2>

                  <!-- نوع الخدمة لو موجود -->
                  <% if (s.type) { %>
                    <div class="small text-muted mb-2">
                      Type:
                      <span class="badge bg-light text-dark border">
                        <%= s.type %>
                      </span>
                    </div>
                  <% } %>

                  <!-- معلومات السعر / min / max -->
                  <div class="row g-2 mb-3">
                    <div class="col-4">
                      <div class="small text-muted">Rate / 1000</div>
                      <div class="fw-bold">
                        $<%= Number(s.rate || 0).toFixed(4) %>
                      </div>
                    </div>
                    <div class="col-4">
                      <div class="small text-muted">Min</div>
                      <div class="fw-bold"><%= s.min_qty %></div>
                    </div>
                    <div class="col-4">
                      <div class="small text-muted">Max</div>
                      <div class="fw-bold"><%= s.max_qty %></div>
                    </div>
                  </div>

                  <!-- ملاحظة سريعة عن أقل سعر ممكن (optional frontend calc) -->
                  <div class="small text-muted mb-3">
                    Minimum cost ≈
                    $<%= ((Number(s.min_qty || 0) * Number(s.rate || 0)) / 1000).toFixed(4) %>
                  </div>

                  <!-- زر الشراء -->
                  <div class="mt-auto">
                    <a
                      href="/social-checkout/<%= s.id %>"
                      class="btn btn-primary w-100"
                    >
                      <i class="bi bi-cart-plus"></i>
                      Buy this service
                    </a>
                  </div>
                </div>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </div>
  </main>

  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
