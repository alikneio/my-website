<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>
    <% 
      const cat =
        (typeof category !== 'undefined' && category) ||
        (typeof categoryName !== 'undefined' && categoryName) ||
        'Social Media';
    %>
    <%= cat %> - Social Media Services
  </title>

  <link rel="stylesheet" href="/css/bootstrap.css">
  <link rel="stylesheet" href="/css/custom.css">
  <link rel="stylesheet" href="/css/bootstrap-icons/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container my-4">
    <% 
      const catTitle =
        (typeof category !== 'undefined' && category) ||
        (typeof categoryName !== 'undefined' && categoryName) ||
        'Social Media';
    %>

    <div class="d-flex align-items-center justify-content-between mb-3">
      <h2 class="section-title mb-0">
        <i class="bi bi-instagram"></i>
        <%= catTitle %> Services
      </h2>

      <a href="/social-media" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> Back to categories
      </a>
    </div>

    <% if (!services || !services.length) { %>
      <div class="alert alert-warning text-center">
        No services available in this category at the moment.
      </div>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-3 g-3">
        <% services.forEach(function(s){ %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title mb-1"><%= s.name %></h5>

                <p class="text-muted small mb-2">
                  Category:
                  <strong><%= s.category || catTitle %></strong>
                </p>

                <div class="row text-center mb-3">
                  <div class="col-4">
                    <div class="small text-muted">Rate / 1000</div>
                    <div class="fw-bold">$<%= Number(s.rate || 0).toFixed(4) %></div>
                  </div>
                  <div class="col-4">
                    <div class="small text-muted">Min</div>
                    <div class="fw-bold"><%= s.min_qty || s.min || 0 %></div>
                  </div>
                  <div class="col-4">
                    <div class="small text-muted">Max</div>
                    <div class="fw-bold"><%= s.max_qty || s.max || 0 %></div>
                  </div>
                </div>

                <% if (s.description) { %>
                  <p class="small text-muted flex-grow-1">
                    <%= s.description %>
                  </p>
                <% } else { %>
                  <p class="small text-muted flex-grow-1">
                    High-quality social media service with fast delivery.
                  </p>
                <% } %>

                <a href="/social-checkout/<%= s.id %>" class="btn btn-primary w-100 mt-auto">
                  Order now
                </a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/main.js"></script>
</body>
</html>
